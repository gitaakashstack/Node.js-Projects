<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link rel="stylesheet" href="/signup.css">
    <link rel="stylesheet" href="/nav.css">
</head>
<body>
    <%- include('./partial/nav.ejs') %>
    <main>
        <p class=" error_msg <%= errorMsg ? 'show_error' : 'no_error' %>"><%= errorMsg %></p>
        <form method="POST" action="/user/signup" >
            <label for="username">Email</label>
            <input type="email" name="email" placeholder="Enter Your Email" value="<%= errorMsg ? oldInput.email : '' %>"autofocus required>
            <label for="password">Password</label>
            <input type="password" name="password" placeholder="Enter Your Password" value="<%= errorMsg ? oldInput.password : '' %>" required>
            <label for="cnfpassword">Confirm Password</label>
            <input type="password" name="cnfpassword" placeholder="Confirm Password" value="<%= errorMsg ? oldInput.cnfpassword : '' %>" required>
            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
            <input type="submit">
        </form>
    </main>
    
</body>
</html>